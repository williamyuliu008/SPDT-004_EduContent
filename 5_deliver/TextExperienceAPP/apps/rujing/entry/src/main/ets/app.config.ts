/**
 * app.config.ts — {{APP_DISPLAY_NAME}}
 *
 * 品牌：{{BRAND_NAME}} · #{{BRAND_COLOR}}
 */
export const AppConfig = {
  appName: '{{APP_DISPLAY_NAME}}',
  shortDesc: '{{APP_SHORT_DESC}}',
  primaryColor: '#{{BRAND_COLOR}}',
} as const;
