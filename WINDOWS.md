# SPDT-004 开发窗口 v1.0

> **版本**：v1.0（2026-08-21）
> **目的**：把"分层"翻译成"节奏"——不同层用不同的时间窗、不同的准入准出、不同的协作模式。
> **配套**：[LAYERS.md](LAYERS.md)
> **状态**：W21 治理收口首发。

---

## 1. 三个窗口

```
W_low  ── 稳定维护窗 ─── 1-2 周一次 ── L0 修小修
W_mid  ── 演化迭代窗 ─── 每天/每周  ── L1 内容生产 + 工具改进
W_high ── 架构开发窗 ─── 每天多次  ── L2 新功能 + 实验
```

---

## 2. W_low · 稳定维护窗

### 节奏
- **频率**：1-2 周一次（周二/周四上午固定）
- **时长**：2-4 小时
- **参与者**：willi 拍板 + Mavis 执行（无需多人在场）

### 内容范围
- L0 模块的 bug 修复
- L0 模块的性能优化
- L0 模块的文档更新（CHANGELOG、README）
- **联调测试**（手机、rujing APP、跨服务）

### 准入（必须满足其一）
- [ ] L0 模块有 P0/P1 级别 bug
- [ ] 性能回归（响应时间 > 1.5x 基线）
- [ ] 联调测试需要外部资源（手机等）
- [ ] L0 文档缺失/陈旧

### 准出（必须全部满足）
- [ ] 修复/优化已 commit
- [ ] 单元测试 + 集成测试全过
- [ ] 回归测试报告输出
- [ ] L0 CHANGELOG 已更新

### 禁止
- ❌ 任何 L1/L2 范围的工作
- ❌ 任何"顺手改一下"（必须独立 PR）
- ❌ 任何接口破坏性变更（要走 RFC）

---

## 3. W_mid · 演化迭代窗

### 节奏
- **频率**：每天/每周（不固定）
- **时长**：单次 1-2 小时，可一天多次
- **参与者**：Mavis（执行）+ autoclaw（生产 AI，协作）

### 内容范围
- L1 模块的日常迭代
  - CGM SOP 文档更新
  - 知识卡片新增/补全
  - Skill 系统扩展
  - 协作工具改进
- 内容生产
  - 古史 v1-v3 已有产物的微调
  - 墨骨山河 ep01/ep09 的扩展
  - 4_adapt 配置包内容补全
- 治理文档同步

### 准入（满足其一即可）
- [ ] L1 模块有明确待办
- [ ] 内容生产批次有规范可循
- [ ] 协作工具使用中发现小问题

### 准出（必须全部满足）
- [ ] 变更符合"通过 L0 接口消费"原则
- [ ] 增量单元测试通过
- [ ] MANIFEST.yaml / SPDT.yaml 同步更新
- [ ] 失败案例有记录

### 禁止
- ❌ 跨入 L2 范围（除非走 RFC）
- ❌ 大规模重构（必须走 RFC）
- ❌ 接口破坏性变更

---

## 4. W_high · 架构开发窗

### 节奏
- **频率**：每天多次（早/中/晚各 1 段）
- **时长**：单次 1-3 小时
- **参与者**：Mavis（执行）+ willi（关键决策拍板）

### 内容范围
- L2 模块的快速迭代
  - 4_adapt 5 Agent 调度
  - 跨包联动 _04_cross_pack
  - 古史_v4 / 墨骨山河 ep02-08 内容扩产
  - ebook_builder Stage A 改造
  - Skill T4 叙事审计
  - 新 Skill 探索
- 实验性功能
- 架构原型

### 准入（满足其一即可）
- [ ] L2 模块有明确功能目标
- [ ] 实验性原型有设计文档
- [ ] 失败/成功都有可记录数据

### 准出（必须全部满足）
- [ ] 变更记录在 `_changelog.md`
- [ ] 失败案例标记 `STATUS: failed-archived`
- [ ] 与 L0/L1 接口对齐（不能有未声明的依赖）
- [ ] 至少 1 个集成测试跑通

### 禁止（与 L0/L1 物理隔离）
- ❌ 直接修改 L0/L1 内部代码
- ❌ 跨层 `import`（只能通过 `__init__.py` 暴露的接口）
- ❌ 把实验性代码标记为"生产可用"
- ❌ 跳过 CHANGELOG

---

## 5. 窗口切换规则

### 5.1 何时切换窗口

```
常态：W_mid（演化）
 ↓ 检测到 L2 新功能需求
W_high（开发）
 ↓ 关键里程碑（设计/接口冻结）
回到 W_mid（验证）
 ↓ 联调测试或 P0 bug
W_low（稳定）
```

### 5.2 紧急情况（破窗规则）

如果出现以下情况，可以**破窗**进入 W_low 紧急处理：

- [ ] 生产环境故障（rujing APP 卡死/数据丢失）
- [ ] 关键路径测试失败（端到端冒烟）
- [ ] 安全漏洞

破窗后必须：
1. 当天补 RFC 文档说明破窗原因
2. 当周内做一次 post-mortem

### 5.3 窗口冲突仲裁

如果同时有 W_low 和 W_high 需求：
- **P0 优先**：生产故障 > 关键里程碑 > 日常演化
- **L0 优先**：L0 故障 > L2 升级
- **联调优先**：W_low 联调只能等到 willi 在场

---

## 6. 工具配套

| 工具 | 用途 | 状态 |
|:---|:---|:---:|
| `tools/layer_lint.py` | 跨层依赖检查（CI 用） | ✅ v1.0 |
| `tools/window_check.py` | 当前窗口 + 准入准出检查（Windows 优先） | ✅ v1.0 |
| `tools/layer_doc_sync.py` | SPDT.yaml 与实际目录/文件一致性检查 | ✅ v1.0 |
| `.github/PULL_REQUEST_TEMPLATE.md` | PR 模板（含 layer/window 字段） | ✅ v1.0 |
| `.github/workflows/layer_check.yml` | CI 门禁（4 jobs） | ✅ v1.0 |
| `tools/rfc_template.md` | L0 变更的 RFC 申请 | 📋 planned |

---

## 7. 周节奏示例（W21）

| 周一 | 周二 | 周三 | 周四 | 周五 | 周末 |
|:---|:---|:---|:---|:---|:---|
| W_mid | **W_low 联调窗** | W_high × 2 | W_mid | **W_low RFC 评审** | W_high 自由 |

> 关键时段：
> - **周二上午**：W_low 联调窗（手机/APP 可用时段）
> - **周五下午**：W_low RFC 评审（willi 拍板）
> - **其他时段**：W_mid / W_high 自由切换

---

## 8. 文件索引

```
LAYERS.md            ← 分层定义
WINDOWS.md           ← 本文件（窗口节奏）
SPDT.yaml            ← 各模块的 layer 字段
docs/governance/分层治理检查清单.md   ← CI / PR 模板（待写）
```

---

## 9. 版本历史

| 版本 | 日期 | 核心变更 |
|:---|:---|:---|
| v1.0 | 2026-08-21 | 首发：W_low/W_mid/W_high 三窗口 + 破窗规则 + 周节奏示例 |
