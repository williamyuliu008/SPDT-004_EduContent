# 质量编译器自证清白 —— 三层验证体系

> 日期：2026-07-04
> 核心问题：如果质量编译器替代人工审查，如何证明编译器自身正确率>99%？

---

## 一、三层自证策略

### 第1层：金标测试集（Ground Truth Suite）

**原理**：已知正确/错误的Scene→编译器判定→比对结果→计算正确率。

**构建方法**：
- **已知缺陷样本（12个）**：回顾DRAM V1-V4的历史bug，每个提取为场景+渲染代码
  - `defect_01`: 文字漂移到box外（T02/DRAM V1）
  - `defect_02`: 维度列空白（DRAM V1 scene_02）
  - `defect_03`: 文字溢出box（DRAM V3 V4）
  - ... 从五版迭代中提取所有已知失败模式
- **已知正确样本（3个）**：T01概念卡片→人工确认正确

**正确率计算**：
```
正确率 = (编译器正确标记PASS的样本数 + 编译器正确标记FAIL的样本数) / 总样本数
```
目标：>99%（22/22样本）

### 第2层：变异注入测试（Mutation Testing）

**原理**：对已知正确的Scene做单点修改注入缺陷→验证编译器能检测到。

**变异算子**：
| 变异类型 | 操作 | 预期G-05结果 |
|---------|------|------------|
| 字段清空 | `title: ""` | G-03 FAIL |
| 长度翻倍 | `label: 40字` | G-04 WARN |
| 删除节点 | `diagram.nodes: []` | G-03 FAIL |
| 裸Manim代码 | 在Scene中用`.next_to()` | G-01 FAIL |
| 删除关键内容 | `wrong_text: ""` | G-03 FAIL |

**正确率计算**：
```
检出率 = 编译器检出变异数 / 注入变异总数
```
目标：>95%（极少量的假阴性可接受）

### 第3层：人工抽检交叉验证（Spot Check Cross-Validation）

**原理**：随机抽5%的渲染结果→人工审查→比对编译器判定→计算一致率。

**抽样策略**：每批次N个Scene，随机抽`max(2, N*5%)`个做人工验证。

**一致率计算**：
```
一致率 = 编译器与人工判定一致的抽样数 / 抽样总数
```
目标：>98%

---

## 二、三层之间的递进关系

```
第1层（金标测试集）→ 每次运行G-05时自动执行
  ↓ 通过（>99%）
第2层（变异注入）→ 每次G-05版本升级时执行
  ↓ 通过（>95%）
第3层（人工抽检）→ 每批次批量渲染后执行
  ↓ 通过（>98%）
  → 编译器可信 → 可以替代人工逐帧审查
```

**三层不全过→不过度信任编译器→保留人工抽检→收集新失败模式→扩充金标测试集→修复编译器→重新自证。**

## 三、实现：金标测试集构建

基于DRAM V1-V4的四版迭代中积累的所有已知失败模式，构建12个金标样本：

### 已知缺陷样本（预期G-05 FAIL）

| ID | 缺陷类型 | 来源 | 预期结果 |
|----|---------|------|------------|
| D01 | 文字在box外（手写Manim定位） | T02/DRAM V1 | G-01 FAIL |
| D02 | 对比表维度列空白 | DRAM V1 scene_02 | G-03 FAIL |
| D03 | 文字溢出box边界 | DRAM V3 V4 | G-04 WARN |
| D04 | diagram节点为空 | DRAM V2 scene_05回退 | G-03 FAIL |
| D05 | steps数组为空 | 构造缺陷 | G-03 FAIL |
| D06 | contrast的wrong_text为空 | 构造缺陷 | G-03 FAIL |
| D07 | title为空字符串 | 构造缺陷 | G-03 FAIL |
| D08 | 画面全黑/空白 | 渲染失败 | G-05B FAIL |
| D09 | 文本像素宽度超260 | 长label场景 | G-04 WARN |

### 已知正确样本（预期ALL PASS）

| ID | 场景 | 来源 |
|----|------|------|
| P01 | T01概念卡片（term_card） | T01已验证通过 |
| P02 | T02三段式（box_diagram） | T02修复版已验证 |
| P03 | T01完整课程（4 Scene） | T01批量渲染通过 |

目标：12/12 (100%)

## 四、从本项目提炼的质量编译器构建方法论

G-01~G-05的构建过程和DRAM视频五版迭代的教训，可提炼为通用原则：

**规则1：静态检查优先，但不足以自证。** G-01/G-03是零成本过滤器。但即使全PASS，渲染仍可能有视觉缺陷。

**规则2：编译器必须进入产物的物理层。** G-04的字符计数漏了文本溢出——字符数不等于像素宽度。G-05通过实际渲染PNG才闭合循环。

**规则3：金标测试集是可信度的锚。** 每发现新bug→新增金标样本→编译器升级→验证全量金标。编译器随失败模式库增长而强化。

**规则4：变异注入是鲁棒性的压力测试。** 对正确场景做微量破坏→验证检出率。不是测典型bug而是测边缘bug。

**规则5：人工抽检随编译器成熟度递减。** 初期（<50样本）：抽检20%+。成熟期（>200样本+>98%正确率）：抽检3-5%。
